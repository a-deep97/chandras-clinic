import React from 'react';
import TopNavbar from './utilities/top-navbar';

const Home = () => {
    return (
        <div>
            <TopNavbar/>      
        </div>
    );
};

export default Home;