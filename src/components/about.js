import React from 'react';
import TopNavbar from './utilities/top-navbar';

const About = () => {
    return (
        <div>
            <TopNavbar/>
        </div>
    );
};

export default About;