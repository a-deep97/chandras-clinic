import React from 'react';
import TopNavbar from './utilities/top-navbar';

const Book = () => {
    return (
        <div>
            <TopNavbar/>
        </div>
    );
};

export default Book;